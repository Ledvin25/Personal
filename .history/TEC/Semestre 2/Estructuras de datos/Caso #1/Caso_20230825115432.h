
changePriority(int pos, int Y); // Cambia la prioridad de la noticia
   // moveToPosition(new_pos);

checkPriority(); // Verifica que las noticias esten ordenadas por prioridad
   // moveToPosition(new_pos);

addNews(string key_words); // Agrega una noticia
   // addData(data);
   // checkPriority(Y);

deleteNews(string key_words); // Elimina una noticia
   // deleteData(data);
   // checkPriority(Y);

showTop5(); // Muestra las 5 noticias con mayor prioridad
   // displayData(pos);

showAll(); // Muestra todas las noticias
   // displayData(pos);

searchWord(string key_words); // Busca una o varias palabras en las noticias
   // displayData(pos);

deleteWords(string key_words); // Elimina noticias que contengan 
   // deleteNews(key_words);