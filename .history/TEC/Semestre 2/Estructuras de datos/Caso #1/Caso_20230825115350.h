
changePriority(int pos, int Y); // Cambia la prioridad de la noticia
   // moveToPosition(new_pos);

checkPriority();
   // moveToPosition(new_pos);

addNews(string key_words);
   // addData(data);
   // checkPriority(Y);

deleteNews(string key_words);
   // deleteData(data);
   // checkPriority(Y);

showTop5();
   // displayData(pos);

showAll();
   // displayData(pos);

searchWord(string key_words);
   // displayData(pos);

deleteWords(string key_words);
   // deleteNews(key_words);