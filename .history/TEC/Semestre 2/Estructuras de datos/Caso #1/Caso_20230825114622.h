showAll();
   // displayData(index);

searchWord(string key_words);
   // displayData(index);

deleteWords(string key_words);
   // deleteData(data);

changePriority(int Y);
   // moveToPosition(new_pos);

checkPriority(int Y);
   // moveToPosition(new_pos);

addNew(string key_words);
   // addData(data);
   // checkPriority(Y);